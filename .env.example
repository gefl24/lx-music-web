# LX Music Web 环境变量配置文件
# 复制此文件为 .env 并修改配置

# 基本配置
NODE_ENV=production          # 运行环境: development, production
PORT=3000                    # 服务端口
DATA_DIR=/app/data           # 数据存储目录
MUSIC_DIR=/app/music         # 音乐存储目录

# 安全配置
# 生产环境必须修改! 使用强密钥
# 生成方法: openssl rand -base64 32
JWT_SECRET=please-change-this-in-production

# 下载配置
MAX_CONCURRENT_DOWNLOADS=3   # 最大并发下载数
DOWNLOAD_RETRY_LIMIT=3       # 下载失败重试次数

# 代理配置 (可选)
# 如果需要使用代理访问音乐平台
# HTTP_PROXY=http://proxy:8080
# HTTPS_PROXY=http://proxy:8080

# 时区设置
TZ=Asia/Shanghai             # 容器时区

# 日志配置
# LOG_LEVEL=info             # 日志级别: debug, info, warn, error
# LOG_FILE=/app/data/app.log # 日志文件路径

# 性能配置
# MAX_REQUEST_SIZE=10mb      # 最大请求大小
# SOCKET_TIMEOUT=30s         # Socket 超时时间

# 数据库配置
# DB_PATH=/app/data/lx-music.db # SQLite 数据库路径

# 缓存配置
# CACHE_ENABLED=true         # 是否启用缓存
# CACHE_TTL=3600             # 缓存过期时间 (秒)

# API 配置
# API_PREFIX=/api            # API 前缀
# API_RATE_LIMIT=100         # API 速率限制 (每分钟请求数)
